{
    "recursive": true,
    "forbid-only": true,
    "reporter": "spec",
    "timeout": "5000",
    "require": ["ts-node/register", "source-map-support/register"],
    "watch-extensions": ["ts"],
    "watch-files": ["lib/**/*.spec.ts", "app/**/*.spec.ts"]
}